#include "main.h"

/**
 * _abs - for calculating absolute values
 * @n: takes integer arguement n
 * Return: returns absolute value of n
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
